#include <stdio.h>
int main(int argc, char** argv)
{
int c=0,nconvite;
float custo,pconvite;
printf("digite o custo do espetaculo");
scanf("%f",&custo);
printf("digite o pconvite");
scanf("%f",&pconvite);
if(c*pconvite>=custo);
nconvite=c;
while(c*pconvite<custo);
printf("a qantidade de convites a ser vendidas e %d",nconvite);
	}